# Custom Changes

上游源码中的侵入式修改清单，用于同步上游时核对二开补丁是否仍然有效。

| 日期 | 文件 | 原因 |
| --- | --- | --- |
| ~~2026-07-13~~<br>2026-08-25 撤除 | ~~`backend/internal/handler/admin/grok_oauth_handler.go`~~ | ~~修复上游 `v0.1.153` 中 Grok 配额重置错误分支触发的 staticcheck SA4023；该接口按设计始终返回“不支持”错误。~~ **已撤除**：上游 `v0.1.181` 的 `cbe258fd1` 已用 `//nolint:staticcheck` 修复同一问题，同步时采用上游版本。 |
| 2026-07-21 | `backend/Makefile` | 固定默认 `LDFLAGS`，避免 shell 环境变量意外覆盖 Go 链接参数，同时保留命令行显式覆盖能力。 |
| 2026-07-21 | `deploy/Makefile` | 固定默认 `LDFLAGS`，避免 shell 环境变量意外覆盖 Go 链接参数，同时保留命令行显式覆盖能力。 |
| 2026-08-08 | `backend/internal/handler/dto/settings.go`<br>`backend/internal/handler/admin/setting_handler.go`<br>`backend/internal/handler/admin/setting_handler_update.go` | Crisp 在线客服设置项（`crisp_enabled` / `crisp_website_id`）的 DTO 字段、读取与更新映射、Website ID UUID 校验。补登记 2026-07 的 `feat(settings): add Crisp customer support widget`。 |
| 2026-08-08 | `backend/internal/server/middleware/security_headers.go` | 在 `requiredCSPDirectiveValues` 注入表中加入 Crisp 域名常量（`https://*.crisp.chat`、`wss://*.relay.crisp.chat`、`wss://*.relay.rescue.crisp.chat`、`blob:`、`data:`）。补登记同上。 |
| 2026-08-08 | `backend/internal/config/config.go`<br>`deploy/config.example.yaml` | 将 Crisp 域名补入 `DefaultCSPPolicy` 默认策略字符串。上游 `0.1.172`（提交 `8e102b3a0`）新增不变量测试，要求默认策略与中间件注入表同形，否则示例配置会误导自建用户。 |
| ~~2026-08-08~~<br>2026-08-09 撤除 | ~~`frontend/src/api/__tests__/admin.system.rollback.spec.ts`~~ | ~~**修上游 bug**：上游 `35b5edb24` 给 `rollback()` 请求加了 `timeout` 选项却未同步本文件断言，`v0.1.162`~`v0.1.172` 纯净 main 上持续失败。补全断言第三参数（`UPDATE_REQUEST_TIMEOUT_MS` 未导出，测试内镜像其值）。~~ **已撤除**：上游 `0.1.173` 的 `85fb77615` 自行修复了同一问题，同步时取上游版本，本文件回归与 main 完全一致。 |
| 2026-08-25<br>2026-08-26 更新 | `backend/internal/handler/dto/types.go`<br>`backend/internal/handler/dto/mappers.go` | 用量明细 DTO 的请求级 `output_tokens_per_second` 统一表示解码速度：`output_tokens × 1000 / (duration_ms - first_token_ms)`。仅 `output_tokens > 0`、`duration_ms > 0`、`first_token_ms` 非空且 `duration_ms > first_token_ms` 时有值，否则为 `nil` 并序列化为 `null`。`AdminUsageLog` 靠嵌入继承，未复制逻辑。 |
| 2026-08-25<br>2026-08-26 更新 | `backend/internal/pkg/usagestats/usage_log_types.go`<br>`backend/internal/repository/usage_log_repo_stats.go` | `UsageStats` 的 `average_output_tokens_per_second` 按上述有效条件计算逐请求解码速度的**算术平均**（非总量加权），`AVG` 与 `output_tokens_per_second_samples` 使用完全相同的过滤条件；首 Token 平均及其样本数语义不变。仅写入 `GROUPING SETS` 总计行，端点拆分、可空扫描和 JSON 字段形状不变。 |
| 2026-08-25<br>2026-08-26 更新 | `backend/internal/server/api_contract_test.go`<br>`backend/internal/repository/usage_log_repo_request_type_test.go` | 同步 API 契约期望，并以 sqlmock 锁定解码时长分母及 `AVG` / `COUNT` 的相同有效样本条件。**同步上游时注意**：这两处会随上游改动冲突，需要重新对齐列顺序、SQL 与期望 JSON。 |
| 2026-08-25<br>2026-08-26 更新 | `frontend/src/types/index.ts`<br>`frontend/src/api/admin/usage.ts`<br>`frontend/src/utils/format.ts` | 前端类型保留解码速度相关可空字段为可选（兼容新前端访问旧后端）；格式化器继续统一两位小数、`tok/s` 单位与空值占位，仅更新字段和 JSDoc 语义。 |
| 2026-08-25<br>2026-08-26 更新 | `frontend/src/components/admin/usage/UsageTable.vue`<br>`frontend/src/components/admin/usage/UsageStatsCards.vue` | 延迟单元格第三行和性能统计卡统一展示“解码速度”（复用现有单元格，**不新增独立列**）；明细标签、数值及统计卡复用同一口径提示，明确扣除首 Token 等待，样本数为 0 时显示 `-`。 |
| 2026-08-25<br>2026-08-26 更新 | `frontend/src/views/user/UsageView.vue`<br>`frontend/src/views/admin/UsageView.vue`<br>`frontend/src/i18n/locales/{zh,en}/dashboard.ts` | 用户 CSV 与管理员 XLSX 在 Duration 之后导出解码速度（纯数值、无效样本留空），表头统一为“解码速度 / Decoding Speed”；CSV 沿用硬编码英文表头，XLSX 走 i18n。 |
| 2026-08-26 | `backend/internal/config/config.go`<br>`backend/internal/config/config_test.go`<br>`deploy/config.example.yaml`<br>`README.md` | 新增 `gateway.openai_ws.ingress_ping_interval_seconds`：默认 30 秒、0 关闭、负数拒绝启动；每次 ping 固定 5 秒超时，仅在完成一轮后等待下一条业务消息时运行，成功 ping 不重置 300 秒业务 idle 上限。 |
| 2026-08-26 | `backend/internal/service/openai_ws_client_read.go`<br>`backend/internal/service/openai_ws_client_read_test.go` | 共享客户端 WS reader 在保持唯一常驻 reader 的同时管理最多一个可取消 ingress ping；业务帧、idle timeout、控制取消与 lease 丢失均会取消并等待 ping 退出。ping 失败以 `GoingAway` 专用 cause marker 归类为客户端断连并保留底层错误，业务 idle 的 `NormalClosure` 语义不变。 |
| 2026-08-26 | `backend/internal/service/openai_ws_forwarder_ingress.go`<br>`backend/internal/service/openai_ws_v2_passthrough_adapter.go`<br>`backend/internal/service/openai_ws_forwarder_ingress_session_test.go`<br>`backend/internal/service/openai_ws_http_bridge_test.go`<br>`backend/internal/service/openai_ws_v2_passthrough_lifecycle_test.go`<br>`backend/internal/service/openai_ws_v2/passthrough_relay_internal_test.go` | legacy、ctx_pool、HTTP bridge 的 continuation read 与 passthrough `waitingForNextTurn` 共用 ingress ping；首包和活跃 turn 保持禁用。模式测试锁定配置传播、跨周期存活、终止事件激活边界、ping/业务 idle 错误分类及不触发 upstream 账号失败上报。 |
