# Custom Changes

上游源码中的侵入式修改清单，用于同步上游时核对二开补丁是否仍然有效。

| 日期 | 文件 | 原因 |
| --- | --- | --- |
| ~~2026-07-13~~<br>2026-08-25 撤除 | ~~`backend/internal/handler/admin/grok_oauth_handler.go`~~ | ~~修复上游 `v0.1.153` 中 Grok 配额重置错误分支触发的 staticcheck SA4023；该接口按设计始终返回“不支持”错误。~~ **已撤除**：上游 `v0.1.181` 的 `cbe258fd1` 已用 `//nolint:staticcheck` 修复同一问题，同步时采用上游版本。 |
| 2026-07-21 | `backend/Makefile` | 固定默认 `LDFLAGS`，避免 shell 环境变量意外覆盖 Go 链接参数，同时保留命令行显式覆盖能力。 |
| 2026-07-21 | `deploy/Makefile` | 固定默认 `LDFLAGS`，避免 shell 环境变量意外覆盖 Go 链接参数，同时保留命令行显式覆盖能力。 |
| 2026-08-08 | `backend/internal/handler/dto/settings.go`<br>`backend/internal/handler/admin/setting_handler.go`<br>`backend/internal/handler/admin/setting_handler_update.go` | Crisp 在线客服设置项（`crisp_enabled` / `crisp_website_id`）的 DTO 字段、读取与更新映射、Website ID UUID 校验。补登记 2026-07 的 `feat(settings): add Crisp customer support widget`。 |
| 2026-08-08 | `backend/internal/server/middleware/security_headers.go` | 在 `requiredCSPDirectiveValues` 注入表中加入 Crisp 域名常量（`https://*.crisp.chat`、`wss://*.relay.crisp.chat`、`wss://*.relay.rescue.crisp.chat`、`blob:`、`data:`）。补登记同上。 |
| 2026-08-08 | `backend/internal/config/config.go`<br>`deploy/config.example.yaml` | 将 Crisp 域名补入 `DefaultCSPPolicy` 默认策略字符串。上游 `0.1.172`（提交 `8e102b3a0`）新增不变量测试，要求默认策略与中间件注入表同形，否则示例配置会误导自建用户。 |
| ~~2026-08-08~~<br>2026-08-09 撤除 | ~~`frontend/src/api/__tests__/admin.system.rollback.spec.ts`~~ | ~~**修上游 bug**：上游 `35b5edb24` 给 `rollback()` 请求加了 `timeout` 选项却未同步本文件断言，`v0.1.162`~`v0.1.172` 纯净 main 上持续失败。补全断言第三参数（`UPDATE_REQUEST_TIMEOUT_MS` 未导出，测试内镜像其值）。~~ **已撤除**：上游 `0.1.173` 的 `85fb77615` 自行修复了同一问题，同步时取上游版本，本文件回归与 main 完全一致。 |
| 2026-08-25<br>2026-08-26 更新 | `backend/internal/handler/dto/types.go`<br>`backend/internal/handler/dto/mappers.go` | 用量明细 DTO 的请求级 `output_tokens_per_second` 统一表示解码速度：`output_tokens × 1000 / (duration_ms - first_token_ms)`。仅 `output_tokens > 0`、`duration_ms > 0`、`first_token_ms` 非空且 `duration_ms > first_token_ms` 时有值，否则为 `nil` 并序列化为 `null`。`AdminUsageLog` 靠嵌入继承，未复制逻辑。 |
| 2026-08-25<br>2026-08-26 更新 | `backend/internal/pkg/usagestats/usage_log_types.go`<br>`backend/internal/repository/usage_log_repo_stats.go` | `UsageStats` 的 `average_output_tokens_per_second` 按上述有效条件计算逐请求解码速度的**算术平均**（非总量加权），`AVG` 与 `output_tokens_per_second_samples` 使用完全相同的过滤条件；首 Token 平均及其样本数语义不变。仅写入 `GROUPING SETS` 总计行，端点拆分、可空扫描和 JSON 字段形状不变。 |
| 2026-08-25<br>2026-08-26 更新 | `backend/internal/server/api_contract_test.go`<br>`backend/internal/repository/usage_log_repo_request_type_test.go` | 同步 API 契约期望，并以 sqlmock 锁定解码时长分母及 `AVG` / `COUNT` 的相同有效样本条件。**同步上游时注意**：这两处会随上游改动冲突，需要重新对齐列顺序、SQL 与期望 JSON。 |
| 2026-08-25<br>2026-08-26 更新 | `frontend/src/types/index.ts`<br>`frontend/src/api/admin/usage.ts`<br>`frontend/src/utils/format.ts` | 前端类型保留解码速度相关可空字段为可选（兼容新前端访问旧后端）；格式化器继续统一两位小数、`tok/s` 单位与空值占位，仅更新字段和 JSDoc 语义。 |
| 2026-08-25<br>2026-08-26 更新 | `frontend/src/components/admin/usage/UsageTable.vue`<br>`frontend/src/components/admin/usage/UsageStatsCards.vue` | 延迟单元格第三行和性能统计卡统一展示“解码速度”（复用现有单元格，**不新增独立列**）；明细标签、数值及统计卡复用同一口径提示，明确扣除首 Token 等待，样本数为 0 时显示 `-`。 |
| 2026-08-25<br>2026-08-26 更新 | `frontend/src/views/user/UsageView.vue`<br>`frontend/src/views/admin/UsageView.vue`<br>`frontend/src/i18n/locales/{zh,en}/dashboard.ts` | 用户 CSV 与管理员 XLSX 在 Duration 之后导出解码速度（纯数值、无效样本留空），表头统一为“解码速度 / Decoding Speed”；CSV 沿用硬编码英文表头，XLSX 走 i18n。 |
| 2026-08-26 | `backend/internal/config/config.go`<br>`backend/internal/config/config_test.go`<br>`deploy/config.example.yaml`<br>`README.md` | 新增 `gateway.openai_ws.ingress_ping_interval_seconds`：默认 30 秒、0 关闭、负数拒绝启动；每次 ping 固定 5 秒超时，仅在完成一轮后等待下一条业务消息时运行，成功 ping 不重置 300 秒业务 idle 上限。 |
| 2026-08-26 | `backend/internal/service/openai_ws_client_read.go`<br>`backend/internal/service/openai_ws_client_read_test.go` | 共享客户端 WS reader 在保持唯一常驻 reader 的同时管理最多一个可取消 ingress ping；业务帧、idle timeout、控制取消与 lease 丢失均会取消并等待 ping 退出。ping 失败以 `GoingAway` 专用 cause marker 归类为客户端断连并保留底层错误，业务 idle 的 `NormalClosure` 语义不变。 |
| 2026-08-26 | `backend/internal/service/openai_ws_forwarder_ingress.go`<br>`backend/internal/service/openai_ws_v2_passthrough_adapter.go`<br>`backend/internal/service/openai_ws_forwarder_ingress_session_test.go`<br>`backend/internal/service/openai_ws_http_bridge_test.go`<br>`backend/internal/service/openai_ws_v2_passthrough_lifecycle_test.go`<br>`backend/internal/service/openai_ws_v2/passthrough_relay_internal_test.go` | legacy、ctx_pool、HTTP bridge 的 continuation read 与 passthrough `waitingForNextTurn` 共用 ingress ping；首包和活跃 turn 保持禁用。模式测试锁定配置传播、跨周期存活、终止事件激活边界、ping/业务 idle 错误分类及不触发 upstream 账号失败上报。 |
| 2026-08-27 | `backend/internal/service/service_tier_billing.go`<br>`backend/internal/service/upstream_response_model.go`<br>`backend/internal/service/openai_gateway_chat_completions_raw.go`<br>`backend/internal/service/openai_gateway_responses_chat_fallback.go` | **客户端 fast 优先于上游回显**（仅 OpenAI 侧）。上游 `75faedda9` + `f06bf181d`（首发于 `v0.1.180`）让上游回显的 `service_tier: "default"` 无条件覆盖客户端请求的 fast，导致实际按 Fast 跑的请求按基准档计费、少收费。改动两层：`resolvedOpenAIUpstreamServiceTierFromObserver` 新增最高优先级分支——出站 body（policy filter/force **之后**）为 `priority`/`fast` 时直接采用，不再读 observer 回显；`ApplyOpenAIServiceTierBillingResolution` 对 `priority`/`fast` 豁免「只降不升」，返回 `Downgraded=false`。新增共享 helper `isPriorityBillingServiceTier` 供两处共用。后两个文件**仅改注释**（原文写“上游回显优先”，与新实现矛盾）。**边界**：`ResolveBillingServiceTier`（两侧共用原语）、`ApplyForwardServiceTierBillingResolution`（Anthropic 入口）、`anthropicSpeedServiceTier` 一律未动；`flex` 与其余档位判定顺序不变；policy filter 删掉字段后出站 tier 为 nil，仍不按 Fast 计费；未请求 fast 时上游回显 priority 也不升档。**同步上游时注意**：这两处逻辑上游仍在改，冲突时需重新确认「出站 fast 优先」这一不变量是否被上游重构绕过。 |
| 2026-08-27 | `backend/internal/service/openai_fast_service_tier_test.go`<br>`backend/internal/service/service_tier_billing_test.go`<br>`backend/internal/service/openai_gateway_record_usage_test.go`<br>`backend/internal/service/openai_ws_forwarder_v2_test.go`<br>`backend/internal/service/openai_ws_http_bridge_test.go`<br>`backend/internal/service/openai_ws_v2_passthrough_lifecycle_test.go` | 上条改动的测试。反转 6 条锁定旧行为的用例并同步改名（`*UpstreamEchoesDefault_OverridesRequestFast` → `*RequestFastWinsOverUpstreamDefaultEcho` 等），覆盖 HTTP 非流式/流式、CC 桥接、WS v2、WS HTTP bridge（canonical `priority` 与别名 `fast` 各一条）与 `RecordUsage`；`TestApplyServiceTierBillingResolutionOnlyRewritesDowngrades` 拆为 openai / anthropic 两组子测试。新增 `TestPassthroughLifecycle_RequestFastWinsOverUpstreamDefaultServiceTier`（passthrough 的 tier 取自出站 body、不经 observer 解析函数，只受计费层豁免影响，为此给 lifecycle server 加了 `startPassthroughLifecycleServerWithHooks` 变体以拿到 `AfterTurn` 的结果）与 `TestForward_ResponsesFlexUnaffectedByFastBillingExemption`（锁定 flex 不受豁免影响）。反转后的用例均补断言 `UpstreamResponseServiceTier` 仍记录回显供审计、下行 body 不被改写；`RecordUsage` 用例加了 priority > base 的 fixture guard 防止断言退化为同义反复。**同步上游时注意**：这些文件会随上游改动冲突，需逐条重新对齐。 |
| 2026-08-28 | `backend/internal/custom/sessionarchive/`<br>`backend/migrations/231_session_archive_foundation.sql`<br>`backend/migrations/232_session_archive_correlation_columns.sql`<br>`backend/migrations/233_session_archive_correlation_indexes_notx.sql`<br>`backend/migrations/234_session_archive_ref_ordering.sql`<br>`backend/migrations/235_session_archive_correlation_fences.sql`<br>`backend/migrations/236_session_archive_storage_backends_compat.sql`<br>`backend/migrations/237_session_archive_storage_backends_finalize.sql` | 新增默认关闭的会话归档模块及 forward-only schema：窄投影、策略、有限队列、三 backend 加密 CAS、导出、删除/保留期/GC、物理孤儿对账和迟到事件 correlation fence。236 增加 per-Blob backend、PG object/chunk 表及兼容约束；237 仅在所有实例换用四列 SQL 后精确移除 legacy 唯一约束。同步上游时须保留迁移编号、唯一约束、advisory lock 顺序和 `*_notx.sql` 注册。 |
| 2026-08-28 | `backend/internal/config/config.go`<br>`backend/internal/repository/s3_client.go`<br>`backend/internal/repository/{migrations_runner.go,session_archive_storage_phase_compat.go,session_archive_storage_phase_finalize.go}`<br>`backend/cmd/server/{main.go,wire.go,wire_gen.go}`<br>`backend/internal/{handler,service,server/middleware}/wire.go`<br>`backend/internal/server/http.go`<br>`README.md`<br>`deploy/{config.example.yaml,DOCKER.md,APPLE_CONTAINER.md}` | 接入 S3/filesystem/PostgreSQL 配置、集中 store registry、按 Blob backend 路由、依赖注入、HTTP 监听后的限时异步启动、自检与停机取消。普通构建只自动应用 236；带 `session_archive_storage_finalize` build tag 的第二阶段构建才应用 237。归档配置或存储故障保持 fail-open，不阻断网关。 |
| 2026-08-28 | `backend/internal/handler/{gateway_handler.go,gateway_handler_chat_completions.go,gateway_handler_responses.go,openai_chat_completions.go,openai_gateway_handler.go,gemini_v1beta_handler.go,session_archive_capture.go,session_archive_request_parts.go}`<br>`backend/internal/service/{session_archive_http_attempt.go,openai_plugin_transport.go}` | 在 Messages、Chat Completions、Responses、Gemini 的既有 body/response 热路径采集原始请求、逐 part 附件/工具、客户端可见响应及真实出站 Attempt。响应 sink 按首次写入时的实际 Content-Type 惰性创建；重试/failover 连续编号，转换正文仅在策略开启时有界读取。同步协议 handler/transport 时须重新核对采集点仍位于转换前原始请求、真实出站和客户端写入边界。 |
| 2026-08-28 | `backend/internal/service/{openai_ws_forwarder.go,openai_ws_forwarder_ingress.go,openai_ws_v2_passthrough_adapter.go,openai_ws_v2/passthrough_relay.go,grok_audio.go,openai_live.go,openai_live_types.go}`<br>`backend/internal/handler/{grok_audio.go,openai_live.go,session_archive_capture.go}` | 为 Responses WS 全模式、Grok Realtime 和 OpenAI Live 增加非阻塞归档事件。Turn/raw/attempt/downstream/terminal 顺序独立于计费，连接退出会补终态并释放许可；Live 明确仅覆盖控制面。同步 relay、重试或断连逻辑时须复核同 Turn Attempt 与终态去重。 |
| 2026-08-28 | `backend/internal/pkg/ctxkey/ctxkey.go`<br>`backend/internal/{handler,service,repository}/usage*`<br>`backend/internal/securityaudit/{prompt_types.go,prompt_snapshot.go,prompt_repository.go,prompt_event_repository.go}`<br>`backend/internal/{handler,service,repository}/ops*` | 在 Usage、Prompt Audit staging/event、Ops 与归档之间双写并查询 `correlation_request_id`，不改写原 `request_id` 或计费幂等约束。同步 DTO、SQL 列序或异步 worker 时须保留关联字段传播及旧记录可空兼容。 |
| 2026-08-28 | `backend/internal/server/{http.go,router.go,routes/admin.go}`<br>`backend/internal/server/middleware/{audit_log.go,step_up.go,wire.go,request_logger.go,session_archive_audit.go,session_archive_download_rate_limit.go}`<br>`backend/internal/service/{audit_log.go,audit_log_service.go}`<br>`backend/internal/securityaudit/{prompt_handler.go,prompt_handler_test.go}` | 注册会话归档管理 API、单次导出票据下载和固定限流；Session Archive 敏感操作及 Prompt Audit 正文允许管理员 JWT/Admin API Key，不再挂本模块自定义 always-step-up。同步必要审计必须先于正文读取/变更，正文 no-store，下载 ticket 仍按路由模板脱敏且消费失败不可回放；全局/其他功能的普通 step-up 与 `EnforceStepUpAlways` 保留。 |
| 2026-08-28 | `backend/internal/service/{ops_alert_evaluator_service.go,ops_models.go,ops_port.go,ops_service.go}`<br>`backend/internal/handler/admin/ops_alerts_handler.go`<br>`frontend/src/views/admin/ops/`<br>`frontend/src/i18n/locales/{zh,en}/admin/ops.ts` | 将归档队列丢弃、存储失败、pending/GC backlog 纳入现有 Ops 运行状态和告警规则，并补充 correlation 关联入口。同步 Ops metric 白名单、handler DTO 或告警编辑器时须保留归档指标映射。 |
| 2026-08-28 | `frontend/src/features/session-archive/`<br>`frontend/src/{router/index.ts,components/layout/AppSidebar.vue}`<br>`frontend/src/features/prompt-audit/`<br>`frontend/src/{views/admin/UsageView.vue,components/admin/usage/}`<br>`frontend/src/api/admin/{usage.ts,ops.ts}`<br>`frontend/src/i18n/locales/{zh,en}/admin/` | 新增会话列表、时间线、逐 part 正文、策略/运行状态、原生 ticket 导出与关联导航；Session Archive 与 Prompt Audit 详情页不再发起 TOTP step-up，正文仍按需读取且关闭详情立即清空。Usage/Ops 补 correlation query hydration、反向入口和 backend 运行状态；同步时须保留列表不预取正文与关闭即清空边界。 |
| 2026-08-29 | `backend/internal/service/openai_billing_profile.go`<br>`backend/internal/service/{billing_service.go,openai_gateway_usage.go,gateway_usage_billing.go,account_stats_pricing.go}`<br>`backend/cmd/repair-gpt56-subscription-billing/` | GPT-5.6 按上游账号计费模式区分 ChatGPT/Codex 订阅 credits 与 API token pricing：订阅关闭 272K API 阶梯、Fast 固定为 Standard 的 2.5x；API Key 保持 Priority 2x 与长上下文 2x/1.5x。用户计费、shadow 父账号与 `account_stats_cost` 共享 profile 及长上下文路径选择；新增默认 dry-run、drift 闸门、值收敛和聚合重算的一次性修数 CLI。**同步上游时注意**：高冲突点是 `billing_service.go` 中 `applyModelSpecificPricingPolicy` 之后的 profile 策略顺序，以及 `account_stats_pricing.go` 的函数签名扩展。历史工具 `backend/cmd/repair-openai-fast-service-tier/account_stats.go` 复刻了旧账号统计分支，不会因此编译失败但已发生逻辑漂移；将来复用前必须同步其 `modelFileStatsCandidates` 口径。 |
| 2026-08-29 | `backend/internal/service/openai_billing_profile_test.go`<br>`backend/internal/service/{billing_service_test.go,account_stats_pricing_test.go,openai_alpha_search_billing_test.go,openai_gateway_cn_fixes_test.go,openai_gateway_search_surcharge_test.go}`<br>`backend/cmd/repair-gpt56-subscription-billing/repair_test.go` | 上条改动的测试：覆盖 OAuth/Setup Token/API Key 在 272K 边界、Standard/Fast、分组与账号开关组合下的费用矩阵，HTTP/Responses WS、shadow、账号统计与聚合口径，以及 API fast 别名；既有私有函数调用显式补零值 profile，锁定 GPT-5.5/GPT-5.4、搜索和国产供应商路径不变；CLI 用例锁定 Fast 上调、长上下文下调与重跑幂等。 |
| 2026-08-31 | `backend/internal/service/openai_fast_policy_test.go` | 上游 0.1.184 的流式 TTFT 设置开始经共享 `GetMultiple` 路径读取；补全 Fast Policy 测试仓库 stub 的批量读取语义，避免相关测试依赖全局设置缓存的执行顺序。 |
| 2026-09-03 | `backend/internal/pkg/openai/{constants.go,constants_test.go}`<br>`backend/internal/service/{openai_model_alias.go,openai_codex_transform.go,openai_codex_models_service.go,openai_compat_prompt_cache_key.go,pricing_service.go,model_pricing_resolver.go,billing_service.go,openai_billing_profile.go}`<br>`backend/internal/service/{openai_model_alias_test.go,openai_codex_models_service_test.go,openai_compat_prompt_cache_key_test.go,pricing_service_test.go,model_pricing_resolver_test.go,openai_billing_profile_test.go,openai_fast_service_tier_test.go,openai_gpt56_max_test.go,openai_oauth_model_support_test.go}`<br>`frontend/src/composables/{useModelWhitelist.ts,__tests__/useModelWhitelist.spec.ts}` | 完整适配 OpenAI `gpt-daybreak-blue-latest`：请求与 compact 子请求始终原样透传，由上游路由到 `gpt-5.6-sol`；本地仅在能力、prompt cache、reasoning（含 max/ultra）、verbosity、图像输入、Fast/priority、上下文及计费价卡判断中继承 Sol。Codex picker 仅在分组显式选择且存在可原样透传的可调度 OAuth/Setup Token 账号时合并该别名，不向普通分组无条件发布；若上游 manifest 已带 Daybreak，则保留其元数据、补齐本地支持的 `service_tiers=priority` 与 `additional_speed_tiers=fast`，并继续执行同一双闸门。分组/渠道显式 Daybreak 价优先，未配置时回落 Sol。同步上游时不得把该别名加入转发模型映射。测试同时修正 GPT-5.6 API 长上下文用例，改由真实目录解析路径加载阶梯。 |
| 2026-09-05 | `backend/internal/repository/{usage_log_repo_insert.go,usage_log_repo_query.go,usage_log_repo_insert_shape_unit_test.go,usage_log_session_id_unit_test.go,usage_log_session_id_integration_test.go,ops_repo_args_test.go}`<br>`backend/internal/service/account_stats_pricing_test.go`<br>`frontend/src/views/admin/__tests__/UsageView.spec.ts` | 同步 0.2.1：并存上游 `upstream_request_id` 与二开 `correlation_request_id`，两条手写 INSERT 补齐第 63 个占位符，查询/参数/测试列序保持一致；集成测试锁定三个关联标识独立往返及空值兼容。账号统计保留 GPT-5.6 profile/长上下文开关并传递上游 Fable max 推理倍率；用量导出同时保留解码速度和上游请求 ID。 |
| 2026-09-05 | `backend/internal/service/{openai_codex_models_service.go,openai_codex_models_service_test.go}` | 同步上游 Sol 的 Ultrafast 元数据时，Daybreak 继续通过能力别名继承同一 service tiers；仅影响本地能力判定，转发模型 ID 与分组/账号双闸门不变。 |
